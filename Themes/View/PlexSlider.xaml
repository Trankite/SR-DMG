<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SR_DMG.Source.UI.View">
    <Style TargetType="{x:Type local:PlexSlider}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PlexSlider}">
                    <DockPanel>
                        <DockPanel.Style>
                            <Style TargetType="DockPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Maximum,RelativeSource={RelativeSource TemplatedParent}}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DockPanel.Style>
                        <DockPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource ForeBrush}"/>
                                <Setter Property="FontWeight" Value="{StaticResource FontWeigh}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="10,5,10,5"/>
                            </Style>
                        </DockPanel.Resources>
                        <TextBlock Text="{Binding Title,RelativeSource={RelativeSource TemplatedParent}}" DockPanel.Dock="Left"/>
                        <TextBlock Text="{Binding Value,RelativeSource={RelativeSource TemplatedParent}}" DockPanel.Dock="Right"/>
                        <Slider
                            Value="{Binding Value,RelativeSource={RelativeSource TemplatedParent}}"
                            Maximum="{Binding Maximum,RelativeSource={RelativeSource TemplatedParent}}"
                            Minimum="1" VerticalAlignment="Center" Delay="500"
                            SmallChange="1" IsSnapToTickEnabled="True">
                            <Slider.Template>
                                <ControlTemplate TargetType="Slider">
                                    <Grid>
                                        <Track Name="PART_Track">
                                            <Track.Resources>
                                                <Style x:Key="Stye_Rack" TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource BackBrush_Minor}"/>
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                    <Setter Property="Height" Value="5"/>
                                                </Style>
                                            </Track.Resources>
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton>
                                                    <RepeatButton.Template>
                                                        <ControlTemplate>
                                                            <Border Style="{StaticResource Stye_Rack}"/>
                                                        </ControlTemplate>
                                                    </RepeatButton.Template>
                                                </RepeatButton>
                                            </Track.DecreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb>
                                                    <Thumb.Template>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Ellipse Fill="{StaticResource BackBrush_Minor}" Width="15" Height="15"/>
                                                        </ControlTemplate>
                                                    </Thumb.Template>
                                                </Thumb>
                                            </Track.Thumb>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton>
                                                    <RepeatButton.Template>
                                                        <ControlTemplate>
                                                            <Border Style="{StaticResource Stye_Rack}"/>
                                                        </ControlTemplate>
                                                    </RepeatButton.Template>
                                                </RepeatButton>
                                            </Track.IncreaseRepeatButton>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Slider.Template>
                        </Slider>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>