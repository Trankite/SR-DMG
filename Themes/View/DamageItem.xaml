<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SR_DMG.Source.UI.Model">
    <Style TargetType="{x:Type local:DamageItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DamageItem}">
                    <Border CornerRadius="15" Background="{DynamicResource BackBrush}">
                        <Grid Width="150" Height="30" Margin="10,0,10,0">
                            <Grid.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource ForeBrush}"/>
                                    <Setter Property="FontWeight" Value="{DynamicResource FontWeigh}"/>
                                    <Setter Property="FontSize" Value="{DynamicResource FontSize}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </Grid.Resources>
                            <TextBlock
                                Text="{Binding Title,RelativeSource={RelativeSource TemplatedParent}}"
                                HorizontalAlignment="Left" Width="50" TextAlignment="Center"/>
                            <TextBlock
                                Text="{Binding Value,RelativeSource={RelativeSource TemplatedParent}}"
                                HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>