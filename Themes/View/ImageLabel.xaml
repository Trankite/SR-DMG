<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SR_DMG.Source.UI.View">
    <Style TargetType="{x:Type local:ImageLabel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ImageLabel}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Image,RelativeSource={RelativeSource TemplatedParent}}" Width="20" Height="20"/>
                        <TextBlock
                            Text="{Binding Title,RelativeSource={RelativeSource TemplatedParent}}"
                            Foreground="{StaticResource ForeBrush}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>