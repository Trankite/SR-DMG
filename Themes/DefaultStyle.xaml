<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:system="clr-namespace:System;assembly=netstandard">
    <Color x:Key="BackColor">#333333</Color>
    <Color x:Key="ForeColor">#FFFFFF</Color>
    <system:Double x:Key="FontSize">15</system:Double>
    <FontWeight x:Key="FontWeigh">Bold</FontWeight>
    <SolidColorBrush x:Key="BackBrush" Color="{StaticResource BackColor}" />
    <SolidColorBrush x:Key="ForeBrush" Color="{StaticResource ForeColor}" />
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource ForeBrush}" />
        <Setter Property="FontWeight" Value="{DynamicResource FontWeigh}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
    </Style>
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource ForeBrush}" />
        <Setter Property="Background" Value="{DynamicResource BackBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BackBrush}" />
        <Setter Property="FontWeight" Value="{DynamicResource FontWeigh}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSize}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
    </Style>
    <Style TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BackBrush}" />
    </Style>
    <Style TargetType="Image">
        <Setter Property="Stretch" Value="Uniform" />
    </Style>
</ResourceDictionary>