<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SR_DMG.Source.UI">
    <Style TargetType="{x:Type local:LabelTextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:LabelTextBox}">
                    <Border CornerRadius="15" Margin="1,1,1,1">
                        <Grid Width="200" Height="30">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Image
                                    Source="{TemplateBinding Image}"
                                    Width="30" Height="30"
                                />
                                <TextBlock Text="{TemplateBinding Label}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox
                                    Text="{TemplateBinding Text}"
                                    TextAlignment="Right"
                                    Width="65" Height="20"
                                />
                                <TextBlock
                                    Text="{TemplateBinding Unit}"
                                    Width="15" Margin="5,0,10,0"
                                />
                            </StackPanel>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>